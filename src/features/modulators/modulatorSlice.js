import { createSlice } from '@reduxjs/toolkit';

export const modulatorSlice = createSlice({
  name: 'modulators',
  initialState: {
    mods: []
  },
  reducers: {

  },
});

// export const { } = modulatorSlice.actions;

export default modulatorSlice.reducer;
